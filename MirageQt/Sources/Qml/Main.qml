import QtQuick
import "ContentView"

// The SwiftUI project treats MainWindow as the application entry point and
// ContentView as its window content.  Keep the same file boundary in QML.
MainWindow {}
